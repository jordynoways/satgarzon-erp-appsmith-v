{
  "gitSyncId": "productos_q_syncproductcategories",
  "id": "PRODUCTOS_q_syncProductCategories",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "INSERT INTO public.product_categories_stel (\n    id,\n    name,\n    description,\n    rate_id,\n    utc_last_modification_date,\n    deleted\n)\nSELECT\n    (value->>'id')::bigint,\n    value->>'name',\n    value->>'description',\n    (value->>'rate-id')::bigint,\n    (value->>'utc-last-modification-date')::timestamp with time zone,\n    (value->>'deleted')::boolean\nFROM json_array_elements('{{ JSON.stringify(Stel_GetProductCategories.data) }}'::json)\nON CONFLICT (id) DO UPDATE SET\n    name = EXCLUDED.name,\n    description = EXCLUDED.description,\n    rate_id = EXCLUDED.rate_id,\n    utc_last_modification_date = EXCLUDED.utc_last_modification_date,\n    deleted = EXCLUDED.deleted,\n    updated_at = NOW();\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "postgres",
      "isAutoGenerated": false,
      "name": "postgres",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "q_syncProductCategories",
    "pageId": "PRODUCTOS",
    "runBehaviour": "ON_PAGE_LOAD",
    "userSetOnLoad": true
  }
}